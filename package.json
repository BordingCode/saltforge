{
  "name": "saltforge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Saltforge — turn-based explore/build vs a hidden AI rival (Battleship endgame). Vanilla TS, no bundler.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
